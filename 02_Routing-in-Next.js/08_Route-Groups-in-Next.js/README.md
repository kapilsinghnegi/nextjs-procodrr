# Route Groups in Next.js

If we want to group routes together, we can use route groups in Next.js. To group rotes together we will just create a directory with the name in the following format `(route-group-name)` and move the routes to that directory.
