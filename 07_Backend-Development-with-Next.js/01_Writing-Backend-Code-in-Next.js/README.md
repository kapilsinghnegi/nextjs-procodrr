# Writing Backend code in Next.js

When we need to create a page, we create a `page.js` file. Similarly, when we need to create a route, we create a `route.js` file. But we can't have both of them in the same directory. We can run any backend code (Node.js) in the `app` directory since it is firstly executed by the Node.js server.
